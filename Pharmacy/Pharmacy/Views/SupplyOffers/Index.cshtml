@model IEnumerable<Pharmacy.Models.Entities.SupplyOffer>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Offer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count(); i++)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => Model.ToList()[i].Offer)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@Model.ToList()[i].Id">Edit</a> |
                    <a id=@string.Concat("detailoffer",i) asp-action="Details" asp-route-id="@Model.ToList()[i].Id">Details</a> |
                    <a id=@string.Concat("deleteoffer",i) asp-action="Delete" asp-route-id="@Model.ToList()[i].Id">Delete</a> |
                    <a asp-action="AcceptAsync" asp-route-id="@Model.ToList()[i].Id">Accept</a>
                </td>
            </tr>
        }
    </tbody>
</table>
