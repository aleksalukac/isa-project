
@model Pharmacy.Models.DTO.SupplyOrderModelDTO


@{
    ViewData["Title"] = "Create";
    ViewData["ActivePage"] = "SupplyOrders";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>SupplyOrder</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" class="form-group" name="SupplyOrders" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <!--<div class="form-group">
        <label asp-for="PharmacyId" class="control-label"></label>
        <select asp-for="PharmacyId" class="form-control" asp-items="ViewBag.PharmacyId"></select>
    </div>
        <div class="form-group">
            <label asp-for="DeleveryDate" class="control-label"></label>
            <input asp-for="DeleveryDate" class="form-control" />
            <span asp-validation-for="DeleveryDate" class="text-danger"></span>
        </div>-
        <div class="form-group">
            <input type="submit" value="Create" class="btn btn-primary" />
        </div>-->
            <div class="form-group">
                <label asp-for="DateExpired" class="control-label"></label>
                <input asp-for="DateExpired" class="form-control" />
                <span asp-validation-for="DateExpired" class="text-danger"></span>
            </div>

                @for (int i = 0; i < Model.SupplyItems.Count; i++)
                {
                    <div class="form-group">
                        <label asp-for="SupplyItems[i].Drug.Name">@Model.SupplyItems[i].Drug.Name Drug.Name</label>

                        <input asp-for="SupplyItems[i].ExtraQuantity" type="number" />

                    </div>
                }

            <!-- <div>
        <a class="nav-link text-dark" asp-controller="SupplyItems" asp-action="Create">Add Drug</a>
    </div>-->
            <button type="submit" class="btn btn-primary">Save Changes</button>

        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

